<ide_selection>The user selected the lines 14 to 26 from /Users/golamrabbani/expatal/PropContext/optimisation-checklist.md:

**Your Goal**

Build an engine that produces a single Context Markdown File per property. That's a living, self-updating document containing every fact an AI agent needs to act. Dense, structured, traced to its source, surgically updated without destroying human edits. Think CLAUDE.md, but for a building, plus it writes itself.

**Why this is hard**

1. Schema alignment: "owner" is called Eigentümer, MietEig, Kontakt, or owner depending on the source system. You must resolve identities across ERPs.

2. Surgical updates: when an new email arrives you can't generate a whole new file. Regenerating the file destroys human edits and burns tokens. You must patch exactly the right section.

3. Signal vs. noise: 90% of emails are irrelevant. The engine must judge what belongs in the context and what doesn't.


This may or may not be related to the current task.</ide_selection>
**Your Goal**

Build an engine that produces a single Context Markdown File per property. That's a living, self-updating document containing every fact an AI agent needs to act. Dense, structured, traced to its source, surgically updated without destroying human edits. Think CLAUDE.md, but for a building, plus it writes itself.

**Why this is hard**

1. Schema alignment: "owner" is called Eigentümer, MietEig, Kontakt, or owner depending on the source system. You must resolve identities across ERPs.

2. Surgical updates: when an new email arrives you can't generate a whole new file. Regenerating the file destroys human edits and burns tokens. You must patch exactly the right section.

3. Signal vs. noise: 90% of emails are irrelevant. The engine must judge what belongs in the context and what doesn't.
--- read the code not just the md files, and tell me how much of it we are following

---

<ide_selection>The user selected the lines 1 to 14 from /Users/golamrabbani/expatal/PropContext/optimisation-checklist.md:
**Criteria for a strong solution**

- generalize beyond the provided dataset and data format
- resolve easy information conflicts automatically and involve humans where ambiguity actually matters
- preserve provenance at the fact level and update automatically when source facts change

This is not a challenge about dumping markdown into folders or building a documentation chatbot. It is about designing a context base that is explainable, editable, robust under change, and useful in practice. Involve humans when it matters, and take over their work as much as possible where it does not.

**Other Product Thoughts**

- cover both graph construction and retrieval
- treat the virtual file system as a product surface, not just storage
- optimize for long-term maintainability by humans and machines


This may or may not be related to the current task.</ide_selection>
ok now compare with these too **Criteria for a strong solution**

- generalize beyond the provided dataset and data format
- resolve easy information conflicts automatically and involve humans where ambiguity actually matters
- preserve provenance at the fact level and update automatically when source facts change

This is not a challenge about dumping markdown into folders or building a documentation chatbot. It is about designing a context base that is explainable, editable, robust under change, and useful in practice. Involve humans when it matters, and take over their work as much as possible where it does not.

**Other Product Thoughts**

- cover both graph construction and retrieval
- treat the virtual file system as a product surface, not just storage
- optimize for long-term maintainability by humans and machines
---

---

ok let's level up then. but again make sure to follow SOLID, DRY, YAGNI principles

---

do what you would do in production ready app

---

By the way; i spoke to another team working on this challenge. They are using summarization approach, and files.naming convention for search. On static corpus, their approach is comparable to ours. But id lf.we can ahow flexibility using .md files schema updates on the fly, that would probably.make us a win!

---

i don't understand , we didn't had that before?

---

so how doees it differ from "surgically updated without destroying human edits. "

---

so i am thiking more like, if user changes a line in markdown, i wnat to keep it as is, but now that i am thinking more, i feel like maybe seeing this md files are not even important at all, this all just acts as memory layer

---

Thought 1: "preserve human-changed lines verbatim." Already built. Patcher rule: bullets/rows w/ **KEY:** prefix = agent-managed (upsert by key). Anything else = human (passed through _replace_section_lines unchanged). Plus # Human Notes h1 = hard boundary, refused. So PM edits a non-keyed bullet inside ## Open Issues? Survives every future patch. That's the "surgical" guarantee = already in ops.py:86-101. Done. --- so rihgt now i see no option for human to edit markdown from the app

---

yes

---

run it

---

ho do i edit

---

but instead of this, wouldn't it be better if human can edit anywhere in the file?